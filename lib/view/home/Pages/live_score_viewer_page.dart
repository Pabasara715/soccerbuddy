import 'package:flutter/material.dart';

class LiveScoreViewerPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Live Score Viewer'),
      ),
      body: Center(
        child: Text('Live Score Viewer Page Content'),
      ),
    );
  }
}
