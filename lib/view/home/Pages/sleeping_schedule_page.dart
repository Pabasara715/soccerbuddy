import 'package:flutter/material.dart';

class SleepingSchedulePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Sleeping Schedule'),
      ),
      body: Center(
        child: Text('Sleeping Schedule Page Content'),
      ),
    );
  }
}
