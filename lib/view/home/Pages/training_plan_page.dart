import 'package:flutter/material.dart';

class TrainingPlanPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Training Plan'),
      ),
      body: Center(
        child: Text('Training Plan Page Content'),
      ),
    );
  }
}
